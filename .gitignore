__pycache__/
*.py[cod]
.venv/
.ruff_cache/
*.egg-info/

# Clips and derived artefacts. Video does not belong in git, and every file under
# work/ is reproducible from a clip plus a stage.
data/clips/*
!data/clips/.gitkeep
work/*
!work/.gitkeep

# SN-Calibration-2023, 2.9 GB of training images for the segmenter. Downloaded, not
# authored -- see "Getting the training data back" in PLAN.md.
data/calib2023/

# Model weights, downloaded on first run.
models/
*.pt

# SoccerNet credentials. Under NDA - never committed, never pasted into a transcript.
.env
